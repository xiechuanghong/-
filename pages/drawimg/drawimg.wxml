<!--pages/drawimg/drawimg.wxml-->
<view class='page'>

  <canvas style="{{img}}" canvas-id='can'></canvas>

   <button open-type='share' class="btn" bindtap='saveImg' >马上分享</button>   

   <image bindtap='previewImg' wx:if="{{url}}" src="{{url}}"></image>  
  
</view>
